module.exports = {
  name: 'turn',
  description: 'Turns a message upside down.',
  aliases: ['t', 'upsidedown', 'updown'],
  async run(client, message, args) {
    // Discord
    const Discord = require('discord.js');
    // Upside down function
    function upsideDown(message) {
      
    }
  }
}
